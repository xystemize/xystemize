import { Module } from '@nestjs/common';
import { Name } from '<%= projectName %>/app-core';
import express from 'express';

import { initializeApiServer } from '@xystemize/backend';

export const <%= name %> = express();

@Module({
  imports: [],
})
export class <%= name %>Module {}

initializeApiServer({
  apiInstance: <%= name %>,
  functionName: Name.<%= nameLowerCase %>,
  module: <%= name %>Module,
});
