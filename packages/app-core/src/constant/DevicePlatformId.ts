/*
 * Current AccountTypeId values should not be change.
 * Changing values might result bugs on codes that uses these values.
 */
export enum DevicePlatformId {
  NoId = 0,
  AppleIos,
  Android,
  Web,
}
